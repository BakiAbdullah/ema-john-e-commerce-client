/** FireBase Project with Ema-Jhon Step by Step :---------------
 
 * 1. create a firebase project
 * 2. create a web app 
 * 3. npm i fireabse
 * 4. save firebase config and export default app from config file
 * 5. Build > Authentication > get started > Enable Sign in Method (Email/password) / (Google sign in)
 * 6. Create Sign in and Login route
 * 7.Then go to firebase docs > build > authentication > web > get Started
*/

/** Using ContextAPI to share auth info allover the apps
 * 1. Create a context provider file
 * 2. Create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider at main.jsx / root.jsx
 * 
*/

/** Hosting in Firebase
 * 1. npm install -g firebase-tools (Just 1 Time)
 * 2. firebase login
 * 
 * 
 * For Each project one time:
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase (arrow key and select by spacebar)
 * 4. existing project
 * 5. select the project carefully
 * 6. which project as public directory: dist =====> vite(dist)
 * 7. Single page application: yes
 * 8. Set up automatic builds with github: No
 * 
 * 
 * For Every time deploy run these Command:
 * 1. npm run build
 * 2. firebase deploy
*/ 